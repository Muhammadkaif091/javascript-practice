var fristNumber = Number(prompt("enter your frist number"));
var lastNumber = Number(prompt("enter your last number"));

alert(" additional result :" + (fristNumber + lastNumber) );
alert(" Subtraction result :" + (fristNumber - lastNumber) );
alert(" multiplication result :" + (fristNumber * lastNumber) );
alert(" division result :" + (fristNumber / lastNumber) );